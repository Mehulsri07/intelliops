id: restart-pod
name: Restart Pod
match_rule: "hypothesis == 'error-spike'"
steps:
  - action: restart
    note: "kubectl rollout restart deploy/{service}"
hitl_mode: hitl
reversible: true
rollback_steps: []
symptoms: "error spikes in logs, crash loops, wedged or hung process, elevated 5xx errors, stuck workers"
